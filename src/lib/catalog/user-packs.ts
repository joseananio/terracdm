import type { SignalPackManifest } from "./types";

// Add trusted, code-reviewed packs here when embedding TerraCDM in another
// application. UI-created configuration intentionally remains out of scope.
export const userSignalPackManifests: SignalPackManifest[] = [];
